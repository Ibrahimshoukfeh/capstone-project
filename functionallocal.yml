# bundle:
#   name: dbx_capstone
 
# run_as:
#   user_name: ${workspace.current_user.userName}
 
# # ONLY YAML bundle definitions go here
# include:
#   - deploy/pipeline/**/*.yml

 
# variables:
#   environment:
#     description: "Deployment environment (dev/uat/prod)"
 
# resources:
#   jobs:
#     dbx_capstone-env-prep-job:
#       name: "dbx_capstone Environment Prep ${workspace.current_user.userName}"
 
#       tasks:
#         - task_key: catalog-prep
#           notebook_task:
#             notebook_path: code/environment/catalog_prep.py
#             base_parameters:
#               env: ${bundle.target}
 
#         - task_key: metadata-loader
#           depends_on:
#             - task_key: catalog-prep
#           notebook_task:
#             notebook_path: code/environment/metadata_loader.py
#             base_parameters:
#               env: ${bundle.target}
 
# targets:
#   dev:
#     workspace:
#       host: https://adb-693586493268953.13.azuredatabricks.net
#       root_path: /Workspace/Users/${workspace.current_user.userName}/dbx_capstone
#     variables:
#       environment: "dev"
 
#   uat:
#     workspace:
#       host: https://adb-693586493268953.13.azuredatabricks.net
#       root_path: /Workspace/Users/${workspace.current_user.userName}/dbx_capstone
#     variables:
#       environment: "uat"